(** Main teamproject library module *)

(** [greet name] greets the given [name] *)
let greet name = Printf.sprintf "Welcome to %s's project!" name

(** [version] returns the current version *)
let version = "1.0.0"
